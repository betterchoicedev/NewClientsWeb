import React from 'react';

/**
 * Subtle fixed ambient glow — stays viewport-stable so scroll sections don't clash.
 * (Rendered inside the scrolling main; kept minimal — no stacked orbs per section.)
 */
export default function FlowCanvas() {
  return null;
}
