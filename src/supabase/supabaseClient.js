/**
 * @deprecated Supabase is only used on the server (see server/index.js).
 * Use ../lib/apiClient.js for authenticated API calls from the browser.
 */

export const supabase = null;
export const supabaseSecondary = null;
